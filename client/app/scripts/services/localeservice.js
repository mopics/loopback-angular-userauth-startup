'use strict';

/**
 * @ngdoc service
 * @name clientApp.localeService
 * @description
 * # localeService
 * Service in the clientApp.
 */
angular.module('clientApp')
  .service('localeService', ['$locale', function ( $locale, $http ) {
    // AngularJS will instantiate a singleton by calling "new" on this function
    
  }]);
